# This file contains invalid settings along with correct ones
# The file with syntax errors should not be handled by psmoveinput
# despite the fact that some values are set correctly

# this is correct setting
MOVE_COEFF_X = 1

# Empty lines are ok, but lines containing nonsense are not
blahblah # like this
MOVE_COEFFX = 1 # or this
MOVE_COEFF_X = test # or this


